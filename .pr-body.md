tier-3
tier-3-approved

```evidence
Risk Tier: 3
Title: Sprint 37 — Retry Patching v1 (Bounded, Deterministic)

Summary:
Introduces deterministic retry patch planning and patch application into the orchestrator retry path without modifying eligibility logic.

Scope:
- patchPlanner (governanceErrorCode → canonical ops)
- patchApplier (deterministic command execution, stop-on-failure)
- canonicalPrBody builder (stable tier + evidence block formatting)
- Execution report schema extensions (retryAttempt, patchPlan, patchOutcomeCode, etc.)
- Full unit + integration test coverage

Affected Paths:
control-plane/orchestrator/retry/**
control-plane/orchestrator/report/**
control-plane/orchestrator/cli/**
control-plane/orchestrator/swarm-task.ts

Determinism Statement:
- No randomness
- No timestamps
- Stable ordering of ops
- Dry-run produces zero side effects
- Canonical JSON output

Tests:
Vitest suites for patchPlanner, patchApplier, swarm-task, and CI normalize integration.
All tests passing locally.
```
