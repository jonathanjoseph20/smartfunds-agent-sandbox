FROM node:22-alpine
WORKDIR /app

COPY control-plane/cockpit/package*.json ./
RUN npm ci

COPY control-plane/cockpit ./

ENV SMARTFUNDS_RUNTIME_BASE_URL=http://runtime:3100
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
