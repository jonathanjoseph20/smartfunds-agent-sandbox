tier-3

```evidence
Risk Tier: 3
Justification: Introduces bounded deterministic Swarm runtime executor with explicit CLI invocation. Creates PRs via sealed mutationKernel. No retry linkage. No background execution. Deterministic hashing and branch behavior enforced.
Affected Paths: control-plane/swarms/**, control-plane/cli/**, package.json, tests
Tests Added: swarmExecutor unit + integration + idempotency tests; CLI tests
Determinism Statement: No timestamps/UUIDs/randomness; canonicalStringify + sha256; deterministic branch naming; single-pass execution; no retry coupling.
```
